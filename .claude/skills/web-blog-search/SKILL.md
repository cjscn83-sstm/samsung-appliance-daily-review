---
name: web-blog-search
description: 네이버 블로그 외 일반 웹·티스토리·브런치·카페·커뮤니티에서 지정 일자의 삼성 가전 후기를 수집한다. web-blog-researcher가 사용한다.
---

# Web Blog Search

## 목적

네이버 블로그를 제외한 다양한 출처에서 **지정 일자** D에 게시된 삼성 가전 후기를 수집한다.

## 입력 인자

- `date` (필수): 수집 대상 일자, `YYYY-MM-DD` 형식.
- 인자가 없으면 어제를 기본값으로 사용.

## 절차

1. 카테고리 트리에서 검색 키워드 목록을 가져온다.
2. 다음 도구를 가능한 범위에서 사용한다:
   - `NaverSearch-search_webkr` (일반 웹)
   - `NaverSearch-search_cafearticle` (카페)
   - `WebFetch` 또는 `fetch` (개별 페이지 본문 확인)
3. 각 키워드로 검색한 결과에서:
   - URL이 `blog.naver.com`이면 **무조건 제외**
   - 게시일이 입력 `date`인 글만 추림
4. 각 글에 출처 유형 태그를 부여:
   - `[블로그]` — 티스토리, 브런치, 워드프레스 등 개인 블로그
   - `[카페]` — 다음/네이버 카페
   - `[커뮤니티]` — 디시, 뽐뿌, 클리앙, 보배드림 등
5. 광고성 신호를 평가해 표시한다.
6. 결과를 **`artifacts/{date}/01-web-raw.md`**에 저장한다.

## 산출물 형식

```md
# 웹/카페/커뮤니티 수집 결과 — {date}

## 수집 통계

- 총 수집: N건
- 출처 유형별: 블로그 N1 / 카페 N2 / 커뮤니티 N3
- 광고성 의심: M건

## TV/영상∙음향

### TV (N건)

| 제목 | URL | 도메인 | 출처유형 | 작성일 | 발췌 | 광고성 |
| --- | --- | --- | --- | --- | --- | --- |
| ... | ... | tistory.com | [블로그] | ... | ... | [정상] |

...

## 수집 0건 품목

- ...
```

## 품질 기준

- `blog.naver.com` URL 0건이어야 한다 (자가 점검 필수).
- 게시일을 확인할 수 없으면 포함하지 않는다.
- 카페/커뮤니티 글은 로그인 필요 여부를 표시 ("로그인 필요" 메모).
- 발췌는 원문 그대로.

## 애매할 때

- 같은 글이 여러 도메인에 미러링되어 있으면 원본 또는 가장 먼저 발견한 것 하나만 유지.
- 출처 유형 판단이 애매하면 도메인을 우선해 `[블로그]`로 표시.
- 검색이 막힌 도메인은 "접근 제한" 메모만 남기고 다음으로 진행.
